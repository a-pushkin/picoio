{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "group": "build",
            "label": "echo",
            "type": "shell",
            "command": "${workspaceRoot}/picoio-firmware/build.sh"
        },
        {
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "label": "echo",
            "type": "shell",
            "command": "${workspaceRoot}/tests/build.sh"
        }
    ]
}
